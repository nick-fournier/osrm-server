apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: osrm-data
  name: osrm-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
